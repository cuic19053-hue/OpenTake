//! opentake-gen — generative AI backend clients.
//!
//! BYOK direct-to-vendor mode and self-hosted proxy mode; replicates the upstream
//! GenerationParams union types and job state machine. Keys live in the OS keychain.
//!
//! Phase 0 scaffold: implementations land in later phases (see docs/ROADMAP.md).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {}
}
