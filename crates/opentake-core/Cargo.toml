[package]
name = "opentake-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Assembly: EditorState (timeline + manifest), command routing, event bus."

[dependencies]
